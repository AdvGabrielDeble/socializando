//go:build windows

package main

import (
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"runtime"
	"sort"
	"strconv"
	"strings"
	"sync"
	"sync/atomic"
	"syscall"
	"unsafe"
)

var (
	winSQLiteDLL       = syscall.NewLazyDLL("winsqlite3.dll")
	procSQLiteOpen     = winSQLiteDLL.NewProc("sqlite3_open")
	procSQLiteClose    = winSQLiteDLL.NewProc("sqlite3_close")
	procSQLiteExec     = winSQLiteDLL.NewProc("sqlite3_exec")
	procSQLiteFree     = winSQLiteDLL.NewProc("sqlite3_free")
	procSQLiteErrmsg   = winSQLiteDLL.NewProc("sqlite3_errmsg")
	procSQLiteLastID   = winSQLiteDLL.NewProc("sqlite3_last_insert_rowid")
	procSQLiteChanges  = winSQLiteDLL.NewProc("sqlite3_changes")
	procSQLiteBusy     = winSQLiteDLL.NewProc("sqlite3_busy_timeout")
)

type queryCollector struct { rows []map[string]string }
var ( collectorSeq uint64; collectorMu sync.Mutex; collectors = map[uintptr]*queryCollector{} )

var sqliteCallback = syscall.NewCallback(func(ctx uintptr, argc uintptr, argv uintptr, colNames uintptr) uintptr {
	collectorMu.Lock(); collector := collectors[ctx]; collectorMu.Unlock(); if collector == nil { return 1 }
	row := map[string]string{}; ptrSize := unsafe.Sizeof(uintptr(0))
	for i := uintptr(0); i < argc; i++ { namePtr := *(*uintptr)(unsafe.Pointer(colNames + i*ptrSize)); valuePtr := *(*uintptr)(unsafe.Pointer(argv + i*ptrSize)); row[cString(namePtr)] = cString(valuePtr) }
	collector.rows = append(collector.rows, row); return 0
})

func cString(ptr uintptr) string { if ptr == 0 { return "" }; buf := make([]byte,0,64); for i:=uintptr(0); i<16*1024*1024; i++ { b:=*(*byte)(unsafe.Pointer(ptr+i)); if b==0 { break }; buf=append(buf,b) }; return string(buf) }
func cBytes(s string)([]byte,error){ if strings.IndexByte(s,0)>=0{return nil,errors.New("texto contém byte NUL")}; return append([]byte(s),0),nil }

type winDB struct { handle uintptr; mu sync.Mutex }
func openWinDB(file string)(*winDB,error){
	if err:=winSQLiteDLL.Load();err!=nil{return nil,fmt.Errorf("SQLite nativo do Windows indisponível: %w",err)}
	b,err:=cBytes(file);if err!=nil{return nil,err};var handle uintptr;rc,_,_:=procSQLiteOpen.Call(uintptr(unsafe.Pointer(&b[0])),uintptr(unsafe.Pointer(&handle)));runtime.KeepAlive(b)
	if int32(rc)!=0||handle==0{if handle!=0{msg:=cStringFromDB(handle);procSQLiteClose.Call(handle);return nil,fmt.Errorf("não foi possível abrir o banco SQLite: %s",msg)};return nil,fmt.Errorf("não foi possível abrir o banco SQLite (código %d)",int32(rc))}
	db:=&winDB{handle:handle};procSQLiteBusy.Call(handle,5000);if err:=db.Exec("PRAGMA foreign_keys=ON; PRAGMA journal_mode=WAL; PRAGMA synchronous=FULL;");err!=nil{db.Close();return nil,err};return db,nil
}
func cStringFromDB(handle uintptr)string{p,_,_:=procSQLiteErrmsg.Call(handle);return cString(p)}
func (db *winDB) Close()error{db.mu.Lock();defer db.mu.Unlock();if db.handle==0{return nil};rc,_,_:=procSQLiteClose.Call(db.handle);if int32(rc)!=0{return fmt.Errorf("falha ao fechar SQLite: %s",cStringFromDB(db.handle))};db.handle=0;return nil}
func (db *winDB) execUnlocked(sql string)error{b,err:=cBytes(sql);if err!=nil{return err};var errPtr uintptr;rc,_,_:=procSQLiteExec.Call(db.handle,uintptr(unsafe.Pointer(&b[0])),0,0,uintptr(unsafe.Pointer(&errPtr)));runtime.KeepAlive(b);if int32(rc)!=0{msg:=cString(errPtr);if errPtr!=0{procSQLiteFree.Call(errPtr)};if msg==""{msg=cStringFromDB(db.handle)};return fmt.Errorf("SQLite: %s",msg)};if errPtr!=0{procSQLiteFree.Call(errPtr)};return nil}
func (db *winDB) Exec(sql string)error{db.mu.Lock();defer db.mu.Unlock();return db.execUnlocked(sql)}
func (db *winDB) queryUnlocked(sql string)([]map[string]string,error){b,err:=cBytes(sql);if err!=nil{return nil,err};id:=uintptr(atomic.AddUint64(&collectorSeq,1));collector:=&queryCollector{};collectorMu.Lock();collectors[id]=collector;collectorMu.Unlock();defer func(){collectorMu.Lock();delete(collectors,id);collectorMu.Unlock()}();var errPtr uintptr;rc,_,_:=procSQLiteExec.Call(db.handle,uintptr(unsafe.Pointer(&b[0])),sqliteCallback,id,uintptr(unsafe.Pointer(&errPtr)));runtime.KeepAlive(b);if int32(rc)!=0{msg:=cString(errPtr);if errPtr!=0{procSQLiteFree.Call(errPtr)};if msg==""{msg=cStringFromDB(db.handle)};return nil,fmt.Errorf("SQLite: %s",msg)};if errPtr!=0{procSQLiteFree.Call(errPtr)};return collector.rows,nil}
func (db *winDB) Query(sql string)([]map[string]string,error){db.mu.Lock();defer db.mu.Unlock();return db.queryUnlocked(sql)}
func (db *winDB) lastIDUnlocked()int64{r,_,_:=procSQLiteLastID.Call(db.handle);return int64(r)}
func (db *winDB) changesUnlocked()int{r,_,_:=procSQLiteChanges.Call(db.handle);return int(int32(r))}
func sqlQ(s string)string{if strings.IndexByte(s,0)>=0{s=strings.ReplaceAll(s,"\x00","")};return "'"+strings.ReplaceAll(s,"'","''")+"'"}
func sqlI(v int64)string{return strconv.FormatInt(v,10)}
func mapInt64(row map[string]string,key string)int64{v,_:=strconv.ParseInt(row[key],10,64);return v}
func mapInt(row map[string]string,key string)int{v,_:=strconv.Atoi(row[key]);return v}

type sqliteStore struct{db *winDB;dataDir string}
func newStore(dataDir string)(Store,error){if dataDir==""{return nil,errors.New("diretório de dados vazio")};if err:=os.MkdirAll(dataDir,0700);err!=nil{return nil,err};dbPath:=filepath.Join(dataDir,"fiscal_saude.sqlite3");db,err:=openWinDB(dbPath);if err!=nil{return nil,err};s:=&sqliteStore{db:db,dataDir:dataDir};if err:=s.initSchema();err!=nil{db.Close();return nil,err};return s,nil}
func (s *sqliteStore)initSchema()error{return s.db.Exec(`
CREATE TABLE IF NOT EXISTS professionals (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,cpf TEXT NOT NULL UNIQUE,occupation_code TEXT NOT NULL,registry TEXT NOT NULL DEFAULT '',active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS payments (id INTEGER PRIMARY KEY AUTOINCREMENT,professional_id INTEGER NOT NULL REFERENCES professionals(id),payer_name TEXT NOT NULL,payer_cpf TEXT NOT NULL,beneficiary_name TEXT NOT NULL,beneficiary_cpf TEXT NOT NULL,payment_date TEXT NOT NULL,amount_cents INTEGER NOT NULL CHECK(amount_cents > 0),description TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'PENDENTE_REVISAO',duplicate_key TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE UNIQUE INDEX IF NOT EXISTS uq_payment_professional_duplicate ON payments(professional_id, duplicate_key);
CREATE INDEX IF NOT EXISTS ix_payment_professional_date ON payments(professional_id, payment_date);
CREATE TABLE IF NOT EXISTS import_restrictions (id INTEGER PRIMARY KEY AUTOINCREMENT,professional_id INTEGER NOT NULL REFERENCES professionals(id),source_name TEXT NOT NULL DEFAULT '',source_sheet TEXT NOT NULL DEFAULT '',source_row INTEGER NOT NULL DEFAULT 0,competence_month INTEGER,competence_year INTEGER,payer_name TEXT NOT NULL DEFAULT '',payer_cpf_raw TEXT NOT NULL DEFAULT '',beneficiary_name TEXT NOT NULL DEFAULT '',beneficiary_cpf_raw TEXT NOT NULL DEFAULT '',payment_date_raw TEXT NOT NULL DEFAULT '',amount_raw TEXT NOT NULL DEFAULT '',issue_code TEXT NOT NULL,issue_reason TEXT NOT NULL,unique_key TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'PENDENTE',resolved_payment_id INTEGER,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,resolved_at TEXT);
CREATE UNIQUE INDEX IF NOT EXISTS uq_restriction_professional_key ON import_restrictions(professional_id, unique_key);
PRAGMA user_version=1;`)}
func(s *sqliteStore)Close()error{return s.db.Close()}
func(s *sqliteStore)AddProfessional(p Professional)(int64,error){v,err:=validateProfessional(p);if err!=nil{return 0,err};s.db.mu.Lock();defer s.db.mu.Unlock();rows,err:=s.db.queryUnlocked("SELECT id FROM professionals WHERE cpf="+sqlQ(v.CPF)+" LIMIT 1;");if err!=nil{return 0,err};if len(rows)>0{return 0,errors.New("CPF profissional já cadastrado")};if err:=s.db.execUnlocked("INSERT INTO professionals(name,cpf,occupation_code,registry,active) VALUES("+sqlQ(v.Name)+","+sqlQ(v.CPF)+","+sqlQ(v.OccupationCode)+","+sqlQ(v.Registry)+",1);");err!=nil{return 0,err};return s.db.lastIDUnlocked(),nil}
func(s *sqliteStore)UpdateProfessional(p Professional)error{v,err:=validateProfessional(p);if err!=nil{return err};if p.ID<=0{return errors.New("profissional inválido")};s.db.mu.Lock();defer s.db.mu.Unlock();rows,err:=s.db.queryUnlocked("SELECT id FROM professionals WHERE id="+sqlI(p.ID)+" LIMIT 1;");if err!=nil{return err};if len(rows)==0{return errors.New("profissional não encontrado")};dup,err:=s.db.queryUnlocked("SELECT id FROM professionals WHERE cpf="+sqlQ(v.CPF)+" AND id<>"+sqlI(p.ID)+" LIMIT 1;");if err!=nil{return err};if len(dup)>0{return errors.New("CPF profissional já cadastrado")};return s.db.execUnlocked("UPDATE professionals SET name="+sqlQ(v.Name)+",cpf="+sqlQ(v.CPF)+",occupation_code="+sqlQ(v.OccupationCode)+",registry="+sqlQ(v.Registry)+",active=1,updated_at=CURRENT_TIMESTAMP WHERE id="+sqlI(p.ID)+";")}
func professionalFromRow(r map[string]string)Professional{return Professional{ID:mapInt64(r,"id"),Name:r["name"],CPF:r["cpf"],OccupationCode:r["occupation_code"],Registry:r["registry"],Active:r["active"]!="0"}}
func(s *sqliteStore)GetProfessional(id int64)(Professional,error){rows,err:=s.db.Query("SELECT id,name,cpf,occupation_code,registry,active FROM professionals WHERE id="+sqlI(id)+" LIMIT 1;");if err!=nil{return Professional{},err};if len(rows)==0{return Professional{},errors.New("profissional não encontrado")};return professionalFromRow(rows[0]),nil}
func(s *sqliteStore)ListProfessionals()([]Professional,error){rows,err:=s.db.Query("SELECT id,name,cpf,occupation_code,registry,active FROM professionals WHERE active=1 ORDER BY name COLLATE NOCASE;");if err!=nil{return nil,err};out:=make([]Professional,0,len(rows));for _,r:=range rows{out=append(out,professionalFromRow(r))};return out,nil}
func restrictionUniqueKey(source string,r RestrictedRow)string{h:=sha256.Sum256([]byte(strings.Join([]string{source,r.SourceSheet,strconv.Itoa(r.SourceRow),strconv.Itoa(r.CompetenceMonth),strconv.Itoa(r.CompetenceYear),r.PayerCPFRaw,r.PaymentDateRaw,r.AmountRaw,r.IssueReason},"\x1f")));return fmt.Sprintf("%x",h[:])}
func(s *sqliteStore)ImportPreview(professionalID int64,preview Preview)(ImportStats,error){if professionalID<=0{return ImportStats{},errors.New("selecione o profissional")};if len(preview.Errors)>0{return ImportStats{},errors.New(strings.Join(preview.Errors,"; "))};s.db.mu.Lock();defer s.db.mu.Unlock();prof,err:=s.db.queryUnlocked("SELECT id FROM professionals WHERE id="+sqlI(professionalID)+" AND active=1 LIMIT 1;");if err!=nil{return ImportStats{},err};if len(prof)==0{return ImportStats{},errors.New("profissional não encontrado")};beforePayRows,err:=s.db.queryUnlocked("SELECT COUNT(*) AS n FROM payments WHERE professional_id="+sqlI(professionalID)+";");if err!=nil{return ImportStats{},err};beforePay:=mapInt(beforePayRows[0],"n");beforeResRows,err:=s.db.queryUnlocked("SELECT COUNT(*) AS n FROM import_restrictions WHERE professional_id="+sqlI(professionalID)+";");if err!=nil{return ImportStats{},err};beforeRes:=mapInt(beforeResRows[0],"n");var b strings.Builder;b.WriteString("BEGIN IMMEDIATE;\n");for _,r:=range preview.Valid{key:=duplicateKey(r.PayerCPF,r.BeneficiaryCPF,r.PaymentDate,r.AmountCents);b.WriteString("INSERT OR IGNORE INTO payments(professional_id,payer_name,payer_cpf,beneficiary_name,beneficiary_cpf,payment_date,amount_cents,description,status,duplicate_key) VALUES("+strings.Join([]string{sqlI(professionalID),sqlQ(r.PayerName),sqlQ(r.PayerCPF),sqlQ(r.BeneficiaryName),sqlQ(r.BeneficiaryCPF),sqlQ(r.PaymentDate),strconv.FormatInt(r.AmountCents,10),sqlQ("Atendimento profissional — importado de planilha"),"'PENDENTE_REVISAO'",sqlQ(key)},",")+ ");\n")};for _,r:=range preview.Restricted{key:=restrictionUniqueKey(preview.SourceName,r);vals:=[]string{sqlI(professionalID),sqlQ(preview.SourceName),sqlQ(r.SourceSheet),strconv.Itoa(r.SourceRow),strconv.Itoa(r.CompetenceMonth),strconv.Itoa(r.CompetenceYear),sqlQ(r.PayerName),sqlQ(r.PayerCPFRaw),sqlQ(r.BeneficiaryName),sqlQ(r.BeneficiaryCPFRaw),sqlQ(r.PaymentDateRaw),sqlQ(r.AmountRaw),sqlQ(r.IssueCode),sqlQ(r.IssueReason),sqlQ(key),"'PENDENTE'"};b.WriteString("INSERT OR IGNORE INTO import_restrictions(professional_id,source_name,source_sheet,source_row,competence_month,competence_year,payer_name,payer_cpf_raw,beneficiary_name,beneficiary_cpf_raw,payment_date_raw,amount_raw,issue_code,issue_reason,unique_key,status) VALUES("+strings.Join(vals,",")+ ");\n")};b.WriteString("COMMIT;");if err:=s.db.execUnlocked(b.String());err!=nil{_ = s.db.execUnlocked("ROLLBACK;");return ImportStats{},err};afterPayRows,err:=s.db.queryUnlocked("SELECT COUNT(*) AS n FROM payments WHERE professional_id="+sqlI(professionalID)+";");if err!=nil{return ImportStats{},err};afterPay:=mapInt(afterPayRows[0],"n");afterResRows,err:=s.db.queryUnlocked("SELECT COUNT(*) AS n FROM import_restrictions WHERE professional_id="+sqlI(professionalID)+";");if err!=nil{return ImportStats{},err};afterRes:=mapInt(afterResRows[0],"n");created:=afterPay-beforePay;if created<0{created=0};dup:=len(preview.Valid)-created;if dup<0{dup=0};createdRes:=afterRes-beforeRes;if createdRes<0{createdRes=0};return ImportStats{CreatedPayments:created,CreatedRestrictions:createdRes,SkippedNoService:len(preview.Skipped),SkippedDuplicates:dup},nil}
func paymentFromRow(r map[string]string)Payment{return Payment{ID:mapInt64(r,"id"),ProfessionalID:mapInt64(r,"professional_id"),Professional:r["professional_name"],PayerName:r["payer_name"],PayerCPF:r["payer_cpf"],BeneficiaryName:r["beneficiary_name"],BeneficiaryCPF:r["beneficiary_cpf"],PaymentDate:r["payment_date"],AmountCents:mapInt64(r,"amount_cents"),Description:r["description"],Status:r["status"],DuplicateKey:r["duplicate_key"]}}
func(s *sqliteStore)ListPayments(f PaymentFilter)([]Payment,error){where:=[]string{"1=1"};if f.ProfessionalID!=0{where=append(where,"p.professional_id="+sqlI(f.ProfessionalID))};if f.Status!=""{where=append(where,"p.status="+sqlQ(f.Status))};if f.Year!=0{where=append(where,"substr(p.payment_date,1,4)="+sqlQ(fmt.Sprintf("%04d",f.Year)))};if f.Month!=0{where=append(where,"substr(p.payment_date,6,2)="+sqlQ(fmt.Sprintf("%02d",f.Month)))};q:="SELECT p.id,p.professional_id,pr.name AS professional_name,p.payer_name,p.payer_cpf,p.beneficiary_name,p.beneficiary_cpf,p.payment_date,p.amount_cents,p.description,p.status,p.duplicate_key FROM payments p JOIN professionals pr ON pr.id=p.professional_id WHERE "+strings.Join(where," AND ")+" ORDER BY p.payment_date DESC,p.id DESC;";rows,err:=s.db.Query(q);if err!=nil{return nil,err};out:=make([]Payment,0,len(rows));for _,r:=range rows{out=append(out,paymentFromRow(r))};return out,nil}
func(s *sqliteStore)ValidatePayments(professionalID int64,year,month int)(int,error){if professionalID<=0||year<2000||month<1||month>12{return 0,errors.New("profissional/competência inválidos")};s.db.mu.Lock();defer s.db.mu.Unlock();q:="UPDATE payments SET status='VALIDADO_RECEITA_SAUDE',updated_at=CURRENT_TIMESTAMP WHERE professional_id="+sqlI(professionalID)+" AND substr(payment_date,1,7)="+sqlQ(fmt.Sprintf("%04d-%02d",year,month))+" AND status='PENDENTE_REVISAO';";if err:=s.db.execUnlocked(q);err!=nil{return 0,err};return s.db.changesUnlocked(),nil}
func restrictionFromRow(r map[string]string)Restriction{return Restriction{ID:mapInt64(r,"id"),ProfessionalID:mapInt64(r,"professional_id"),Professional:r["professional_name"],SourceName:r["source_name"],SourceSheet:r["source_sheet"],SourceRow:mapInt(r,"source_row"),CompetenceMonth:mapInt(r,"competence_month"),CompetenceYear:mapInt(r,"competence_year"),PayerName:r["payer_name"],PayerCPFRaw:r["payer_cpf_raw"],BeneficiaryName:r["beneficiary_name"],BeneficiaryCPFRaw:r["beneficiary_cpf_raw"],PaymentDateRaw:r["payment_date_raw"],AmountRaw:r["amount_raw"],IssueCode:r["issue_code"],IssueReason:r["issue_reason"],Status:r["status"]}}
func(s *sqliteStore)ListRestrictions()([]Restriction,error){rows,err:=s.db.Query("SELECT r.*,p.name AS professional_name FROM import_restrictions r JOIN professionals p ON p.id=r.professional_id WHERE r.status='PENDENTE' ORDER BY COALESCE(r.competence_year,0) DESC,COALESCE(r.competence_month,0) DESC,r.id DESC;");if err!=nil{return nil,err};out:=make([]Restriction,0,len(rows));for _,r:=range rows{out=append(out,restrictionFromRow(r))};return out,nil}
func(s *sqliteStore)ResolveRestriction(id int64,payerName,payerCPF,beneficiaryName,beneficiaryCPF,paymentDate,amount string)(int64,error){payerName=strings.TrimSpace(payerName);if payerName==""{return 0,errors.New("nome do pagador é obrigatório")};pcpf,err:=normalizeCPF(payerCPF);if err!=nil{return 0,errors.New("CPF do pagador inválido")};if strings.TrimSpace(beneficiaryName)==""{beneficiaryName=payerName};if strings.TrimSpace(beneficiaryCPF)==""{beneficiaryCPF=pcpf};bcpf,err:=normalizeCPF(beneficiaryCPF);if err!=nil{return 0,errors.New("CPF do beneficiário inválido")};d,err:=parseUserDate(paymentDate);if err!=nil{return 0,err};cents,err:=parseUserAmount(amount);if err!=nil{return 0,err};key:=duplicateKey(pcpf,bcpf,d,cents);s.db.mu.Lock();defer s.db.mu.Unlock();rows,err:=s.db.queryUnlocked("SELECT * FROM import_restrictions WHERE id="+sqlI(id)+" AND status='PENDENTE' LIMIT 1;");if err!=nil{return 0,err};if len(rows)==0{return 0,errors.New("pendência não localizada ou já resolvida")};r:=restrictionFromRow(rows[0]);dup,err:=s.db.queryUnlocked("SELECT id FROM payments WHERE professional_id="+sqlI(r.ProfessionalID)+" AND duplicate_key="+sqlQ(key)+" LIMIT 1;");if err!=nil{return 0,err};if len(dup)>0{return 0,errors.New("lançamento já existente")};if err:=s.db.execUnlocked("BEGIN IMMEDIATE;");err!=nil{return 0,err};ok:=false;defer func(){if !ok{_ = s.db.execUnlocked("ROLLBACK;")}}();insert:="INSERT INTO payments(professional_id,payer_name,payer_cpf,beneficiary_name,beneficiary_cpf,payment_date,amount_cents,description,status,duplicate_key) VALUES("+strings.Join([]string{sqlI(r.ProfessionalID),sqlQ(payerName),sqlQ(pcpf),sqlQ(beneficiaryName),sqlQ(bcpf),sqlQ(d),strconv.FormatInt(cents,10),sqlQ("Atendimento profissional — pendência corrigida"),"'PENDENTE_REVISAO'",sqlQ(key)},",")+ ");";if err:=s.db.execUnlocked(insert);err!=nil{return 0,err};paymentID:=s.db.lastIDUnlocked();if err:=s.db.execUnlocked("UPDATE import_restrictions SET status='RESOLVIDO',resolved_payment_id="+sqlI(paymentID)+",resolved_at=CURRENT_TIMESTAMP WHERE id="+sqlI(id)+";");err!=nil{return 0,err};if err:=s.db.execUnlocked("COMMIT;");err!=nil{return 0,err};ok=true;return paymentID,nil}
func(s *sqliteStore)MarkExported(ids []int64)error{if len(ids)==0{return nil};s.db.mu.Lock();defer s.db.mu.Unlock();var b strings.Builder;b.WriteString("BEGIN IMMEDIATE;\n");for _,id:=range ids{b.WriteString("UPDATE payments SET status='EXPORTADO_CSV',updated_at=CURRENT_TIMESTAMP WHERE id="+sqlI(id)+" AND status='VALIDADO_RECEITA_SAUDE';\n")};b.WriteString("COMMIT;");if err:=s.db.execUnlocked(b.String());err!=nil{_ = s.db.execUnlocked("ROLLBACK;");return err};return nil}
func(s *sqliteStore)Counts()(map[string]int,error){queries:=map[string]string{"professionals":"SELECT COUNT(*) AS n FROM professionals WHERE active=1;","payments":"SELECT COUNT(*) AS n FROM payments;","restrictions":"SELECT COUNT(*) AS n FROM import_restrictions WHERE status='PENDENTE';"};out:=map[string]int{};keys:=[]string{"professionals","payments","restrictions"};sort.Strings(keys);for _,k:=range keys{rows,err:=s.db.Query(queries[k]);if err!=nil{return nil,err};if len(rows)>0{out[k]=mapInt(rows[0],"n")}};return out,nil}
